@page "/settings/retention"
@inject NavigationManager Nav

<div class="page">
    <SubNav Active="retention" />
    <div class="page-head"><div class="title"><h1>Retention</h1><div class="sub">How long generated files and job history are kept</div></div></div>
    <Card>
        <EmptyState Icon="clock-cog" Title="Retention settings">
            This screen is scaffolded but not part of the prototype spec. Build it here following the same
            pattern — a ConfigGrid of retention windows per artifact type (files, job logs, audit events).
        </EmptyState>
    </Card>
</div>
