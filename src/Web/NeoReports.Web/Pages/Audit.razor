@page "/settings/audit"
@inject NavigationManager Nav

<div class="page">
    <SubNav Active="audit" />
    <div class="page-head"><div class="title"><h1>Audit</h1><div class="sub">Immutable log of executions, edits and access decisions</div></div></div>
    <Card>
        <EmptyState Icon="history" Title="Audit log">
            This screen is scaffolded but not part of the prototype spec. Build it here as a filterable
            Timeline/DataGrid over audit events (see the toggles on the Authentication screen).
        </EmptyState>
    </Card>
</div>
