<Project Sdk="Microsoft.NET.Sdk.Web">

  <PropertyGroup>
    <TargetFramework>net8.0</TargetFramework>
    <IsPackable>false</IsPackable>
    <GenerateDocumentationFile>false</GenerateDocumentationFile>
  </PropertyGroup>

  <!-- Everything this demo needs beyond hosting — the four source packages and their drivers, the
       CSV/XLSX formats, the Local destination, the UI, jobs and the ASP.NET integration — comes from
       AllSourcesShared, which is where the shared setup lives (ADR D71). Only the three types this
       file's Program.cs names directly are referenced here. -->
  <ItemGroup>
    <ProjectReference Include="..\..\..\src\UI\NeoReports.UI\NeoReports.UI.csproj" />
    <ProjectReference Include="..\..\..\src\Integrations\NeoReports.AspNetCore\NeoReports.AspNetCore.csproj" />
    <ProjectReference Include="..\..\NeoReports.Samples.AllSourcesShared\NeoReports.Samples.AllSourcesShared.csproj" />
  </ItemGroup>

</Project>
